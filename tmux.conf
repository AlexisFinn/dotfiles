# List of plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'

# Initialize TMUX plugin manager (keep at the bottom of config)
run '/home/alexis/.config/tmux/plugins/tpm/tpm'
